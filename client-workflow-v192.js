(()=>{
'use strict';
if(window.__denisfitClientWorkflowV192)return;
window.__denisfitClientWorkflowV192=true;
document.documentElement.dataset.denisfitBuild='19.8.1';

const SUPABASE_URL='https://dlaiacatpcgbzmumtggw.supabase.co';
const SUPABASE_KEY='sb_publishable_1_IEt89WvEBEhykAfS7rvw_jF7WwD6l';
const db=window.supabase?.createClient?.(SUPABASE_URL,SUPABASE_KEY,{auth:{persistSession:true,autoRefreshToken:true}});
if(!db)return;
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=v=>new Intl.NumberFormat('ru-RU').format(Number(v||0))+' ₽';
const fmt=v=>v?new Date(v).toLocaleDateString('ru-RU',{day:'2-digit',month:'short',year:'numeric'}):'—';
const dateKey=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
const timeKey=d=>`${String(d.getHours()).padStart(2,'0')}:${String(d.getMinutes()).padStart(2,'0')}`;
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
let lastClientId='',observerAttached=false,queued=false,clientBusy=false,programsBusy=false,nextBusy=false;

function marker(){const b=$('#roleBadge');if(!b||b.dataset.v1981)return;b.dataset.v1981='1';const s=document.createElement('small');s.textContent=' · 19.8.1';s.style.opacity='.6';s.style.fontSize='9px';b.appendChild(s)}
function toast(text,error=false){let el=$('#dfv192Toast');if(!el){el=document.createElement('div');el.id='dfv192Toast';document.body.append(el)}el.textContent=text;el.className=`dfv191-toast show${error?' error':''}`;clearTimeout(el._t);el._t=setTimeout(()=>el.classList.remove('show'),2600)}
function rememberClient(id){if(!id)return;lastClientId=id;try{for(const k of ['df_v192_client','df_v191_client','df_v190_client','df_v188_client'])sessionStorage.setItem(k,id)}catch{}}
function clientId(){if(lastClientId)return lastClientId;try{return sessionStorage.getItem('df_v192_client')||sessionStorage.getItem('df_v191_client')||sessionStorage.getItem('df_v190_client')||sessionStorage.getItem('df_v188_client')||''}catch{return''}}
async function trainer(){const s=await db.auth.getSession(),u=s.data.session?.user;if(!u||u.is_anonymous)return null;const r=await db.from('user_roles').select('role').eq('user_id',u.id).maybeSingle();return !r.error&&r.data?.role==='trainer'?u:null}
function section(label){return $$('.section-title').find(x=>(x.textContent||'').trim()===label)||null}
function clientCard(){return Boolean($('#saveClientPlan')&&$('#assignFromClient')&&section('Абонементы')&&section('Программы'))}
function programsPage(){return Boolean($('#programCreateTemplate')&&$('h1')?.textContent.trim()==='Программы')}

async function getClientPrograms(uid,cid){const q=await db.from('training_programs').select('id,name,description,active,created_at').eq('trainer_id',uid).eq('client_id',cid).eq('active',true).order('created_at',{ascending:false});if(q.error)throw q.error;return q.data||[]}
async function deleteClientProgram(uid,cid,pid){const a=await db.from('training_programs').update({active:false,updated_at:new Date().toISOString()}).eq('id',pid).eq('trainer_id',uid).eq('client_id',cid).select('id').maybeSingle();if(a.error)throw a.error;if(!a.data?.id)throw new Error('Шаблон у клиента не найден');const b=await db.from('assigned_workouts').update({status:'cancelled',updated_at:new Date().toISOString()}).eq('trainer_id',uid).eq('client_id',cid).eq('program_id',pid).eq('status','scheduled');if(b.error)throw b.error}
function renderClientPrograms(rows,uid,cid){const title=section('Программы');if(!title)return;let box=$('#dfv192ClientPrograms');if(!box){box=document.createElement('div');box.id='dfv192ClientPrograms';box.className='card dfv191-programs-card';title.insertAdjacentElement('afterend',box)}box.innerHTML=`<div class="list-row"><div><b>Назначенные шаблоны</b><div class="small muted">Удаление только у этого клиента.</div></div><span class="pill">${rows.length}</span></div>${rows.length?rows.map(x=>`<div class="dfv191-program-row" data-client-program="${x.id}"><div><b>${esc(x.name)}</b><small>${esc(x.description||'Программа клиента')}</small></div><button type="button" class="btn inline danger" data-v192-remove-client="${x.id}" data-name="${esc(x.name)}">Удалить у клиента</button></div>`).join(''):'<div class="empty">Назначенных шаблонов нет.</div>'}`;$$('[data-v192-remove-client]',box).forEach(btn=>btn.onclick=async()=>{if(!confirm(`Удалить «${btn.dataset.name||'шаблон'}» у клиента? Выполненная история сохранится.`))return;btn.disabled=true;try{await deleteClientProgram(uid,cid,btn.dataset.v192RemoveClient);renderClientPrograms(await getClientPrograms(uid,cid),uid,cid);toast('Шаблон удалён у клиента')}catch(e){btn.disabled=false;toast(e.message||'Не удалось удалить шаблон',true)}})}

async function getPackages(uid,cid){const q=await db.from('client_packages').select('id,package_name,total_sessions,used_sessions,entry_mode,amount_paid,paid_at,purchased_at,created_at,status').eq('trainer_id',uid).eq('client_id',cid).order('created_at',{ascending:false});if(q.error)throw q.error;return q.data||[]}
function renderPackages(rows){const title=section('Абонементы');if(!title)return;const blocks=rows.filter(x=>x.entry_mode==='purchase'&&Number(x.total_sessions)>1),singles=rows.filter(x=>x.entry_mode==='purchase'&&Number(x.total_sessions)===1),sessions=blocks.reduce((s,x)=>s+Number(x.total_sessions||0),0),paid=blocks.reduce((s,x)=>s+Number(x.amount_paid||0),0);let box=$('#dfv192PackageStats');if(!box){box=document.createElement('div');box.id='dfv192PackageStats';box.className='card dfv191-package-stats';title.nextElementSibling?.insertAdjacentElement('afterend',box)}if(!box)return;box.innerHTML=`<div class="list-row"><div><b>История покупки блоков</b><div class="small muted">Текущий остаток не считается новой покупкой.</div></div><span class="pill">${blocks.length} блок.</span></div><div class="dfv191-stats-grid"><div><span>Куплено блоков</span><strong>${blocks.length}</strong></div><div><span>Тренировок куплено</span><strong>${sessions}</strong></div><div><span>Оплачено</span><strong>${money(paid)}</strong></div></div><div class="dfv191-history-title">Покупки</div>${blocks.length?blocks.map((x,i)=>`<div class="dfv191-purchase-row"><span class="dfv191-purchase-num">${blocks.length-i}</span><div><b>${esc(x.package_name||`Блок ${x.total_sessions}`)}</b><small>${fmt(x.paid_at||x.purchased_at||x.created_at)} · ${Number(x.total_sessions||0)} трен.${Number(x.amount_paid||0)>0?` · ${money(x.amount_paid)}`:''}</small></div></div>`).join(''):'<div class="empty">Новых покупок блоков пока нет.</div>'}${singles.length?`<div class="small muted dfv191-singles-note">Разовых покупок: ${singles.length}</div>`:''}`}
async function enhanceClient(){if(!clientCard()||clientBusy)return;const cid=clientId();if(!cid)return;if($('#dfv192ClientPrograms')&&$('#dfv192PackageStats'))return;clientBusy=true;try{const u=await trainer();if(!u)return;const [p,k]=await Promise.all([getClientPrograms(u.id,cid),getPackages(u.id,cid)]);if(clientCard()&&clientId()===cid){renderClientPrograms(p,u.id,cid);renderPackages(k)}}catch(e){console.error('v192 client',e);toast('Не удалось загрузить управление клиентом',true)}finally{clientBusy=false}}

async function getTemplates(uid){const q=await db.from('program_templates').select('id,name,description,created_at,template_exercises(id)').eq('trainer_id',uid).order('created_at',{ascending:false});if(q.error)throw q.error;return q.data||[]}
function renderTemplates(rows,uid){let box=$('#dfv192TemplateManager');if(!box){box=document.createElement('div');box.id='dfv192TemplateManager';box.className='card dfv191-template-manager';$('#programCreateTemplate')?.closest('.client-actions-grid')?.insertAdjacentElement('afterend',box)}if(!box)return;box.innerHTML=`<div class="list-row"><div><b>Мои шаблоны</b><div class="small muted">Открыть или удалить шаблон.</div></div><span class="pill">${rows.length}</span></div>${rows.length?rows.map(x=>`<div class="dfv191-template-row" data-v192-template="${x.id}"><div><b>${esc(x.name)}</b><small>${x.template_exercises?.length||0} упр. · ${esc(x.description||'Без описания')}</small></div><div class="dfv191-template-actions"><button type="button" class="btn inline secondary" data-v192-open="${x.id}">Открыть</button><button type="button" class="btn inline danger" data-v192-delete="${x.id}" data-name="${esc(x.name)}">Удалить</button></div></div>`).join(''):'<div class="empty">Шаблонов пока нет.</div>'}`;$$('[data-v192-open]',box).forEach(btn=>btn.onclick=()=>{$('#programShowTemplates')?.click();setTimeout(()=>$(`[data-edit-template="${btn.dataset.v192Open}"]`)?.click(),120)});$$('[data-v192-delete]',box).forEach(btn=>btn.onclick=async()=>{if(!confirm(`Удалить шаблон «${btn.dataset.name||''}» из «Программ»? Уже назначенные клиентам программы останутся.`))return;btn.disabled=true;const q=await db.from('program_templates').delete().eq('id',btn.dataset.v192Delete).eq('trainer_id',uid);if(q.error){btn.disabled=false;return toast(q.error.message,true)}renderTemplates(await getTemplates(uid),uid);toast('Шаблон удалён из «Программ»')})}
async function enhancePrograms(){if(!programsPage()||programsBusy||$('#dfv192TemplateManager'))return;programsBusy=true;try{const u=await trainer();if(u)renderTemplates(await getTemplates(u.id),u.id)}catch(e){console.error('v192 programs',e)}finally{programsBusy=false}}

function modal(html){let root=$('#dfv192Modal');if(!root){root=document.createElement('div');root.id='dfv192Modal';document.body.append(root)}root.innerHTML=`<div class="dfv191-modal-back"><div class="dfv191-modal-sheet">${html}</div></div>`;root.classList.add('open');$('.dfv191-modal-back',root).onclick=e=>{if(e.target===e.currentTarget)closeModal()}}
function closeModal(){const x=$('#dfv192Modal');if(x){x.classList.remove('open');x.innerHTML=''}}
async function context(eventId,uid){const [e,p,a]=await Promise.all([db.from('schedule_events').select('id,starts_at,ends_at,training_type,status,notes').eq('id',eventId).eq('trainer_id',uid).single(),db.from('schedule_event_clients').select('client_id,package_id,payment_mode').eq('event_id',eventId),db.from('assigned_workouts').select('client_id,program_id,program_name,day_label,status').eq('trainer_id',uid).eq('schedule_event_id',eventId)]);if(e.error)throw e.error;if(p.error)throw p.error;if(a.error)throw a.error;return{event:e.data,participants:p.data||[],assignments:a.data||[]}}
async function waitCompleted(id,uid){let c=null;for(let i=0;i<10;i++){c=await context(id,uid);if(c.event?.status==='completed')return c;await sleep(250+i*70)}return c}
async function activePackage(uid,cid,id){if(!id)return null;const q=await db.from('client_packages').select('id,total_sessions,used_sessions,status').eq('id',id).eq('trainer_id',uid).eq('client_id',cid).maybeSingle();if(q.error)throw q.error;const p=q.data;return p&&p.status==='active'&&Number(p.total_sessions)-Number(p.used_sessions)>0?p.id:null}
async function createNext(ctx,date,time,uid){const start=new Date(`${date}T${time}:00`);if(Number.isNaN(start.getTime()))throw new Error('Проверь дату и время');const duration=Math.max(15,Math.round((new Date(ctx.event.ends_at)-new Date(ctx.event.starts_at))/60000)||55),end=new Date(start.getTime()+duration*60000);const overlap=await db.from('schedule_events').select('id').eq('trainer_id',uid).neq('status','cancelled').lt('starts_at',end.toISOString()).gt('ends_at',start.toISOString()).limit(1);if(overlap.error)throw overlap.error;if(overlap.data?.length&&!confirm('На это время уже есть тренировка. Всё равно записать?'))return null;const ev=await db.from('schedule_events').insert({trainer_id:uid,starts_at:start.toISOString(),ends_at:end.toISOString(),training_type:ctx.event.training_type,status:'scheduled',notes:ctx.event.notes||''}).select('id').single();if(ev.error)throw ev.error;try{const parts=[];for(const p of ctx.participants){const pid=p.payment_mode==='package'?await activePackage(uid,p.client_id,p.package_id):null;parts.push({event_id:ev.data.id,client_id:p.client_id,package_id:pid,payment_mode:pid?'package':'single',single_paid:false,paid_amount:null,paid_at:null})}if(parts.length){const q=await db.from('schedule_event_clients').insert(parts);if(q.error)throw q.error}const assigns=ctx.assignments.filter(x=>x.status!=='cancelled').map(x=>({trainer_id:uid,client_id:x.client_id,program_id:x.program_id||null,program_name:x.program_name||'Тренировка',day_label:x.day_label||null,scheduled_date:date,status:'scheduled',schedule_event_id:ev.data.id}));if(assigns.length){const q=await db.from('assigned_workouts').insert(assigns);if(q.error)throw q.error}return ev.data.id}catch(e){await db.from('schedule_events').delete().eq('id',ev.data.id).eq('trainer_id',uid);throw e}}
async function offerNext(id){if(nextBusy||$('#dfv192Modal.open'))return;nextBusy=true;try{const u=await trainer();if(!u)return;const ctx=await waitCompleted(id,u.id);if(ctx?.event?.status!=='completed')return;const old=new Date(ctx.event.starts_at),next=new Date(old);next.setDate(next.getDate()+7);let names=[];if(ctx.participants.length){const q=await db.from('profiles').select('id,full_name').in('id',ctx.participants.map(x=>x.client_id));if(!q.error)names=(q.data||[]).map(x=>x.full_name||'Клиент')}modal(`<div class="dfv191-modal-head"><div><span>СЛЕДУЮЩАЯ ТРЕНИРОВКА</span><h2>Записать сразу?</h2><p>${esc(names.join(' + ')||'Клиент')}</p></div><button id="dfv192Close">×</button></div><div class="dfv191-next-grid"><label>Дата<input id="dfv192Date" type="date" value="${dateKey(next)}"></label><label>Время<input id="dfv192Time" type="time" value="${timeKey(old)}"></label></div><div class="small muted dfv191-next-note">По умолчанию — через неделю в то же время.</div><div class="dfv191-modal-actions"><button id="dfv192Later" class="btn secondary">Не сейчас</button><button id="dfv192Book" class="btn primary">Записать</button></div>`);$('#dfv192Close').onclick=closeModal;$('#dfv192Later').onclick=closeModal;$('#dfv192Book').onclick=async()=>{const b=$('#dfv192Book');b.disabled=true;try{const newId=await createNext(ctx,$('#dfv192Date').value,$('#dfv192Time').value,u.id);if(!newId){b.disabled=false;return}closeModal();toast('Следующая тренировка записана');$('[data-page="schedule"]')?.click()}catch(e){b.disabled=false;toast(e.message||'Не удалось записать',true)}}}catch(e){console.error('v192 next',e)}finally{nextBusy=false}}

function enhance(){if(queued)return;queued=true;setTimeout(()=>{queued=false;marker();enhanceClient();enhancePrograms()},60)}
function attach(){if(observerAttached)return;const main=$('#main');if(!main)return;new MutationObserver(enhance).observe(main,{childList:true});observerAttached=true;enhance()}
document.addEventListener('click',e=>{const c=e.target.closest?.('[data-client]');if(c?.dataset.client)rememberClient(c.dataset.client);const st=e.target.closest?.('[data-event-status]');if(st){const [id,s]=String(st.dataset.eventStatus||'').split('|');if(id&&s==='completed')setTimeout(()=>offerNext(id),250)}if(e.target.closest?.('[data-page="clients"]')||e.target.closest?.('[data-page="programs"]')||e.target.closest?.('#addPackage')||e.target.closest?.('[data-client-template]'))setTimeout(enhance,100)},true);
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',attach,{once:true});else attach();window.addEventListener('pageshow',()=>{attach();enhance()});
})();
